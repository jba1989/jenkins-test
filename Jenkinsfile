pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "hello"
                sh 'docker run hello-world'
            }                
        }
    }
}
